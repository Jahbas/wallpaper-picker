# wallpaper-picker

Simple GTK script to preview and pick wallpapers for `hyprpaper`.

It loads images from `~/Pictures/Wallpapers` and updates your Hyprland wallpaper when you click one.

## Run

```bash
./wallpaper-picker
```

Requires Python, GTK 3 (`PyGObject`), and `hyprpaper`.
